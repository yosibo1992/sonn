[build]
  command = "echo 'No build needed'"
  publish = "."

[build.environment]
  NODE_VERSION = "18"

[[edge_functions]]
  function = "redirect"
  path = "/*"           # ← Tek string, her şeyi yakalar
