[build]
  # Build komutu yok, sadece dosyaları yayınla
  command = "echo 'No build step needed'"
  # Kök dizindeki dosyaları doğrudan yayınla (public klasörü yok)
  publish = "."

[build.environment]
  NODE_VERSION = "18"

[[edge_functions]]
  function = "redirect"
  path = ["/", "/index.html"]
